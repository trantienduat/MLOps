# Prometheus configuration for monitoring
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # MNIST API metrics
  - job_name: 'mnist-api'
    static_configs:
      - targets: ['api:8000']
    metrics_path: '/metrics'
